# Vending Machine Simple API

Simple API vending machine

# BUILD
    ```bash
        go get
        go mod tidy
    ```

# SWAGGER
    ```bash
        swag init
    ```

# RUN
    ```bash
        go run main.go
    ```


# ROUTE
    ```bash
        swagger /swagger/index.html
    ```